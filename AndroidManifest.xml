<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="de.thecamper.android.wkgvertretungsplan"
    android:versionCode="15"
    android:versionName="@string/versionNumber" >

    <uses-sdk
        android:minSdkVersion="8"
        android:targetSdkVersion="16" />

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:name="MyApplication"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name">
        <activity
            android:name=".WKGVertretungsplanActivity"
            android:label="@string/app_name" >
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name="PreferencesActivity"
            android:label="@string/preferenceLabel" >
        </activity>
        <activity
            android:name="MyChangelog"
            android:label="@string/changelogLabel" >
        </activity>
    </application>

</manifest>